model User {
    id        String   @id @default(uuid())
    email     String   @unique
    firstName String
    lastName  String
    age       Int
    address   String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("users")
}
